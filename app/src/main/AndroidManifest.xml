<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.delaroystudios.camera" >

    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Refotografing"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.delaroystudios.camera.MainActivity"
            android:label="Refotografing" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.delaroystudios.camera.CameraActivity" />
        <activity android:name="com.delaroystudios.camera.SelectPointsActivity" />
        <activity android:name="com.delaroystudios.camera.RefotografieActivity" />
    </application>

</manifest>
